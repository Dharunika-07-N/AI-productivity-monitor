# AI Time-Wasting Pattern Detector

## Quick Start
Double-click **`run.bat`** to install dependencies and start the application.

## Manual Start
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run the application:
   ```bash
   python main.py
   ```